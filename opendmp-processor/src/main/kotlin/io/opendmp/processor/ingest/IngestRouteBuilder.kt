package io.opendmp.processor.ingest

import org.apache.camel.builder.RouteBuilder

class IngestRouteBuilder(val ingestRequest: IngestRequest) : RouteBuilder() {
    override fun configure() {

    }
}